<%= form_for(@cross) do |f| %>
  <% if @cross.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cross.errors.count, "error") %> prohibited this cross from being saved:</h2>

      <ul>
      <% @cross.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :male_id %><br>
    <%= f.number_field :male_id %>
  </div>
  <div class="field">
    <%= f.label :female_id %><br>
    <%= f.number_field :female_id %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
